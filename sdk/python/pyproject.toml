[project]
name = "nodepick"
version = "0.1.1"
description = "Official Python SDK for nodepick.ai"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.24.0",
    "mcp>=2.0.0b1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
